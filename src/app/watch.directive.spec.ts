import { WatchDirective } from './watch.directive';

describe('WatchDirective', () => {
  it('should create an instance', () => {
    const directive = new WatchDirective();
    expect(directive).toBeTruthy();
  });
});
